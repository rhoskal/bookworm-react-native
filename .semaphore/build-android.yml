version: v1.0
name: "Build APK"
agent:
  machine:
    type: e1-standard-2
  containers:
    - name: main
      image: semaphoreci/android:28

blocks:
  - name: "Build for Android"
    task:
      jobs:
      - name: 🚀Release
        commands:
          - checkout
          - yarn build:android
